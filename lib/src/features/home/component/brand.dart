import 'package:balanjo_app/src/shared/component/src/header.dart';
import 'package:flutter/material.dart';

class ShopByBrand extends StatelessWidget {
  const ShopByBrand({super.key});

  @override
  Widget build(BuildContext context) {
    return Header(title: "Shop By Brands", child: Container());
  }
}

class Brand extends StatelessWidget {
  const Brand({super.key});

  @override
  Widget build(BuildContext context) {
    // a container with rounded corner 8px
    return Container();
  }
}
